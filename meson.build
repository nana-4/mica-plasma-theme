project(
  'mica-plasma-theme',
  version: '0.1',
  license: 'GPLv2+',
  meson_version: '>= 0.46.0',
  default_options: ['prefix=/usr'],
)

install_dir = join_paths(get_option('datadir'), 'plasma', 'desktoptheme')

install_subdir('mica', install_dir: install_dir)
